<div class="container ">

    {{#if isUser}}
    <a href="/catalog/add" class="btn btn-info">Crear anuncio</a>
    {{/if}}
    
    <div class="class row ">
        <div class="col-md-10">
            <div class="class row catalog-page">
                {{#each allCars}}
                {{>carCard this}}
                {{/each}}
            </div>
        </div>
        <div class="col-md-2">
            {{>filter}}
        </div>
    </div>

</div>

<script src="/javascripts/favorite.js"></script>